// Centralized application version constant. Keep in sync with the workspace
// `package.json` files for each runtime; this single export lets the UI layers
// reference one source of truth instead of hardcoding a literal.
export const MUSHROOM_APP_VERSION = "1.0.0";
