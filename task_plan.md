# 蘑菇 IM 产品实施路线图

## 目标定位

- 目标不是做一个最小可用 IM，而是在当前仓库范围内完成一套可持续演进、可验证、可交付的成熟 IM 产品骨架。
- 产品能力同时覆盖：
  - 本地开发与诊断基线
  - 账号体系与认证
  - 消息链路与会话状态
  - 联系人与群治理
  - 富媒体、互动与通知
  - 质量、运维与交付能力
  - 多端、多设备、账号与平台能力

## 当前状态

- 当前实施阶段：`Stage 10 / 多设备、账号与平台能力`
- 当前阶段状态：`complete`
- 已完成阶段：
  - `Stage 0`
  - `Stage 1`
  - `Stage 2`
  - `Stage 3`
  - `Stage 4`
  - `Stage 5`
  - `Stage 6`
  - `Stage 7`
  - `Stage 8`
  - `Stage 9`
  - `Stage 10`
- 尚未开始的产品阶段：
  - 无

## 后续专题迭代：消息同步治理

### 当前目标

- 进入独立专题迭代。
- 当前目标已升级为完成 `P0` 到 `P3` 全量落地：
  - `P0` 启动同步收口、会话分页、已读语义收紧
  - `P1` 分页消息增量、连续游标、缺口修复
  - `P2` 冷启动最近窗口、按需历史、后台补齐
  - `P3` 稳定游标、私聊最后一条消息已读、基线与观测

### 当前实施切片

- Slice 1：启动同步收口与重复同步消除
  - [x] 登录页不再单独执行一套全量式同步
  - [x] Home / WebSocket 建连后成为统一同步入口
- Slice 2：会话同步翻页
  - [x] `conversation/sync` 客户端支持循环翻页拉取完整会话集合
- Slice 3：消息补拉收紧
  - [x] 仅当服务端已投递序列大于本地已同步序列时才发起 `message/sync`

### 下一批实施项

- Slice 4：自动已读语义收紧
  - [x] 当前激活会话收到消息时，补充窗口可见性与真实展示条件，避免隐藏窗口自动已读
  - [x] 窗口重新聚焦或页面重新可见时，为当前激活会话补做已读同步
- Slice 5：连续游标与缺口治理方案落地
  - [x] 设计并实现本地连续同步游标，避免仅按最大序号推进
  - [x] 为乱序、漏写和补洞建立本地状态与恢复流程
- Slice 6：消息补齐分批与缺口后台修复
  - [x] `message/sync` 客户端按批发送，避免一次把所有会话压进单请求
  - [x] 当前打开会话在进入前优先修复缺口，避免带洞进入已读链路
  - [x] 后台周期性扫描带缺口会话并按优先级补齐
- Slice 7：冷启动最近窗口与后台补齐持久化
  - [x] 首装 / 重装默认只预热最近窗口消息
  - [x] 历史消息改为进入会话后按页回溯
  - [x] 本地持久化后台补齐任务队列并按优先级调度
- Slice 8：稳定游标、已读回执与专项基线
  - [x] 会话同步改为稳定复合游标
  - [x] 消息状态同步改为稳定复合游标并支持分页
  - [x] 私聊最后一条消息已读展示接通
  - [x] 补充消息同步基线脚本与指标文档

### 已完成的本轮专题范围

- 启动同步入口统一：
  - [x] 登录后不再独立跑一轮重同步
  - [x] 暖启动、登录后、重连后统一回到同一条同步路径
- 会话同步正确性：
  - [x] `conversation/sync` 客户端支持完整翻页
  - [x] 会话状态变化不再默认触发消息补拉
- 消息同步安全性：
  - [x] 本地 `last_sync_sequence` 改为“连续已落库序号”
  - [x] 新增 `last_server_sequence` 与 `sync_gap_detected`
  - [x] 实时消息乱序时不再直接把连续游标推进到最大序号
- 已读语义：
  - [x] 自动已读受窗口可见和聚焦条件约束
  - [x] 有缺口时不会越过缺口自动已读
- 缺口恢复：
  - [x] 消息写入后按连续性重新计算同步进度
  - [x] 当前会话可前台优先修洞
  - [x] 后台定时按优先级修洞

### 风险记录

- 当前专题文档中定义的 `P0-P3` 方案已经全部落地到代码、测试或基线文档。
- 后续不再是“消息同步设计欠账”，而是围绕观测精度、生产数据校准和体验细节继续迭代。

## 后续专题迭代：移动端 SQLite 存储与通讯录体验

### 当前目标

- 修复移动端联系人页面通讯录推荐在无数据、无权限和刷新失败时的用户反馈问题。
- 将移动端重数据持久化切换到 `react-native-nitro-sqlite`：
  - SQLite：联系人、会话、消息、消息状态、本地缓存数据
  - MMKV：登录态、当前用户、Token、用户偏好、开关配置、轻量状态
- 不编写旧 MMKV 重数据兼容迁移代码，允许删除数据库重建。
- 补充测试并完成 lint / 测试验证。

### 当前实施切片

- Slice 1：存储边界梳理
  - [x] 确认 `packages/app-core` 通过 `MobileDataRepository` 注入本地数据仓库
  - [x] 确认移动端当前仍使用 JSON/MMKV 保存联系人、会话、消息
- Slice 2：SQLite 仓库实现
  - [x] 新增移动端 SQLite 数据仓库
  - [x] 覆盖联系人、会话、消息、消息状态读写与清理
  - [x] 保持消息排序、隐藏序号、清空消息后的会话进度语义
- Slice 3：移动端接线与通讯录体验
  - [x] 移动端主数据仓库切到 SQLite，轻量状态保留 MMKV
  - [x] 通讯录权限被拒绝或无匹配时给出明确提示
  - [x] 修复通讯录刷新异常时顶部出现 `undefined is not a function`
- Slice 4：测试与验证
  - [x] 补充 SQLite 仓库测试
  - [x] 补充联系人页面空推荐 / 权限提示测试
  - [x] 运行 `pnpm run lint`
  - [x] 运行移动端相关测试

## 后续专题迭代：桌面端聊天媒体缓存

### 当前目标

- 按 `docs/cache.md` 落地 Electron 桌面端媒体缓存能力。
- 默认缓存根目录为 `app.getPath("userData")/media-cache`。
- 目录结构为 `<username>/<yyyy_MM>/<images|files|voice|video|thumbs>`。
- 以主进程为唯一文件系统入口，Renderer 只能通过安全 IPC 访问缓存能力。
- 补充必要测试并完成 lint / 测试验证。

### 当前实施切片

- Slice 1：缓存核心规则
  - [x] 新增可测试的目录、分类、文件名、路径安全纯逻辑
  - [x] 覆盖 username 规范化、`yyyy_MM`、五类目录、扩展名推断测试
- Slice 2：Electron 主进程能力
  - [x] 新增媒体缓存主进程服务
  - [x] 新增 `media_cache` 本地索引表迁移
  - [x] 注册 `media-cache:*` IPC
- Slice 3：Renderer 接线
  - [x] preload 暴露安全 API
  - [x] Web 类型声明补齐
  - [x] 聊天媒体消息读取缓存路径，普通文件打开走主进程
- Slice 4：验证
  - [x] 新增 Electron 测试脚本与测试用例
  - [x] 运行 `pnpm run lint`
  - [x] 运行 Electron 缓存测试

## 后续专题迭代：移动端消息分页与媒体缓存

### 当前目标

- 对齐 `docs/message-media-strategy.md` 中移动端仍缺失的消息分页与媒体缓存策略。
- 移动端打开会话默认渲染最近 50 条，上滑按 50 条追加历史，并使用虚拟化列表。
- 移动端新增 `media_cache` SQLite 索引和磁盘缓存，按账号、远程 URL 与分类去重。
- 图片渲染时自动缓存；≤20MB 普通文件渲染时自动缓存；视频和语音点击时下载缓存。
- 已缓存媒体离线可用，未缓存媒体给出破图、未下载或播放失败提示。

### 当前实施切片

- Slice 1：确认差距与现有架构
  - [x] 核对 `docs/message-media-strategy.md`
  - [x] 确认移动端当前 `ScrollView` 一次渲染全部消息
  - [x] 确认移动端当前媒体直接使用远程 URL
- Slice 2：消息分页基础
  - [x] 扩展移动端 SQLite 仓库支持按 50 条分页读取
  - [x] 聊天详情改用 FlashList/虚拟化列表并上滑追加
- Slice 3：媒体缓存基础设施
  - [x] 新增移动端 `media_cache` 表、缓存服务、SHA-256 去重与并发合并
  - [x] 接入图片、文件、视频、语音下载策略
- Slice 4：离线降级与验证
  - [x] 补充未缓存/下载失败 UI
  - [x] 补充相关移动端测试
  - [x] 运行 `pnpm run lint`

## 阶段总览

| 阶段     | 名称                       | 状态     | 目标                                                    |
| -------- | -------------------------- | -------- | ------------------------------------------------------- |
| Stage 0  | 本地开发与诊断基线         | complete | 打通本地启动、健康检查、依赖可见性与启动失败诊断        |
| Stage 1  | 认证、账号与联系人最小闭环 | complete | 注册、登录、昵称持久化、好友关系链路与基础 UX           |
| Stage 2  | 单聊消息最小闭环           | complete | 私聊文本消息、ack、去重、同步、未读/已读与重连恢复      |
| Stage 3  | 群聊与会话状态             | complete | 建群、拉人、群消息、群成员同步、会话状态同步            |
| Stage 4  | 媒体消息与上传体验         | complete | 图片/文件消息、上传反馈、预览与媒体面板闭环             |
| Stage 5  | 质量系统与回归基线         | complete | 测试、质量门、CI 与人工回归清单                         |
| Stage 6  | 运维、交付与上线准备       | complete | smoke check、压测基线、运维文档、监控与回滚演练         |
| Stage 7  | 消息动作与成熟 IM 能力     | complete | 撤回、回复、转发、收藏、置顶、搜索增强、清空消息治理    |
| Stage 8  | 联系人与群治理能力         | complete | 删除好友、拉黑、群公告、权限、角色、退群/解散等治理闭环 |
| Stage 9  | 富媒体、互动与通知体验     | complete | emoji、音视频入口、通知策略、未读提示与提醒优先级       |
| Stage 10 | 多设备、账号与平台能力     | complete | 资料编辑、会话摘要、全局搜索、附件中心与环境加载修正    |

## 已完成阶段说明

### Stage 0

- 页面显式展示 API / WebSocket 连通状态。
- 启动失败可见，包括：
  - 数据库异常
  - Redis 未启动
  - 对象存储配置缺失
  - 初始化脚本未执行
- 服务端通过 `/healthz` 与 `/readyz` 暴露启动状态与 `503` 原因。

### Stage 1

- 用户注册、登录闭环。
- `nickname` 正确持久化。
- Web / Electron 登录态恢复正确。
- 好友申请、接受与联系人列表同步正确。
- 认证 / 联系人失败态 UX 完整。
- 真实服务链路与最小回归已验证。

### Stage 2

- 单聊文本消息与会话列表闭环。
- 消息发送、ack、去重与 websocket 回执稳定。
- 实时消息展示正确。
- `client_message_id` 去重成立。
- `/message/sync` 增量同步可用。
- `/conversation/read` 权限与状态回写可用。
- Web 刷新 / Electron 重启后状态恢复正确。

### Stage 3

- 支持建群与邀请成员。
- 群成员、群资料与群会话同步正确。
- 群文本消息可正常投递。
- 当前群会话状态在来消息后表现明确：
  - 置顶保留
  - 免打扰保留
  - 归档清除
  - 接收方草稿保留

### Stage 4

- 图片消息闭环。
- 文件消息闭环。
- 上传反馈、失败态与重试提示可见。
- 图片预览可用。
- 媒体面板可刷新。
- 图片 / 文件消息链路已通过可执行验证脚本验证。

### Stage 5

- shared / server 回归测试已补齐关键覆盖。
- `pnpm verify:quality` 已建立。
- CI 已接入稳定质量子集。
- Electron 回归检查清单已补齐。
- 聊天窗口人工回归清单已补齐。

### Stage 6

- 实时链路基线：`server/test/stage6-realtime-baseline.mjs`
- 实时校验命令：`pnpm verify:realtime`

## Stage 7 当前范围

### 明确原则

- 消息不可编辑。
- 不再继续引入消息编辑能力。
- 用户可以在多端看到一致的消息状态，但不修改历史消息内容。

### Stage 7 范围拆分

- 消息撤回
- 回复链路闭环
- 转发消息
- 收藏消息
- 置顶消息
- 会话内搜索增强
- 清空消息治理
- 收藏 / 置顶状态跨端同步

### Stage 7 完成清单

- [x] 撤回消息能力完成，发送方与接收方在同一条消息上都能看到正确状态。
- [x] 回复链路完成，引用消息与原消息的关联展示正确。
- [x] 转发能力可用，并支持在已有会话之间转发消息。
- [x] 收藏消息可用，并有独立查看入口。
- [x] 置顶消息可用，并支持取消置顶。
- [x] 收藏 / 置顶状态可跨端同步。
- [x] 会话内搜索支持文本、文件、收藏、置顶、撤回等筛选。
- [x] 支持清空当前会话消息。

### Stage 7 验证记录

- 已通过校验：
  - `pnpm.cmd run lint`
  - `pnpm.cmd --filter @mushroom/server test`
  - `pnpm.cmd --filter @mushroom/shared test`
  - `pnpm.cmd --filter @mushroom/web exec tsc --noEmit`
  - `pnpm.cmd --filter @mushroom/server verify:stage7`
- 说明：
  - `pnpm verify:ops`
  - `pnpm verify:load`
  - `pnpm verify:realtime`
    这些属于运维与基线验证，不是 Stage 7 的功能回归主校验项。

## 成熟 IM 产品阶段规划

### Stage 8：联系人与群治理能力

- 删除好友
- 拉黑 / 取消拉黑
- 群公告
- 群主 / 管理员角色治理
- 邀请权限与资料权限控制
- 移除成员
- 退群 / 解散群规则细化与验证
- 已完成，并通过可执行治理脚本覆盖关键规则

### Stage 9：富媒体、互动与通知体验

- 表情面板
- emoji / 常用互动入口
- 音频消息入口
- 视频消息入口
- 统一走图片 / 文件附件消息主链路
- 预览能力增强
- 未读标题、桌面通知与提醒策略细化
- 系统通知 / 免打扰通知策略
- `@` 提醒优先级策略

### Stage 10：多设备、账号与平台能力

- 用户资料编辑能力
- 头像、昵称、签名编辑闭环
- 在线状态与会话摘要
- 设备标识与设备摘要展示
- 异常登录能力预留
- 全局消息搜索与跨会话跳转
- 附件中心 / 全局媒体聚合
- 提前加载 `.env`，避免直接导入路径回落到默认 `127.0.0.1:6379`

## Stage 8 完成记录

### 当前目标

- 删除好友
- 拉黑 / 取消拉黑
- 群公告
- 群主 / 管理员角色治理
- 邀请权限与资料权限控制
- 移除成员
- 退群 / 解散群规则细化与验证

### 已完成内容

- 删除好友链路完成，包括：
  - shared API 暴露 `deleteFriend`
  - 通讯录界面可直接删除好友
  - 删除后本地缓存、会话状态与数据库状态同步收口
  - 服务端覆盖核心关系删除与治理验证
- 拉黑 / 取消拉黑能力完成。
- 群治理侧已有公告、角色、权限、移除、退群、转让群主、解散群等能力，并补上可执行验收。

### 阶段结论

- `Stage 8` 已完成，当前仓库范围内的联系人与群治理能力达到既定成熟 IM 目标。
- 下一阶段锚点为 `Stage 9 / 富媒体、互动与通知体验`，现也已完成。

## Stage 9 完成记录

### 当前目标

- 表情面板
- emoji / 常用互动入口
- 音频消息入口
- 视频消息入口
- 统一走图片 / 文件附件消息主链路
- 预览能力增强
- 未读标题、桌面通知与提醒策略细化
- 系统通知 / 免打扰通知策略
- `@` 提醒优先级策略

### 已完成内容

- 输入框已接入可用的表情面板，支持常用 emoji 插入。
- 音频 / 视频消息入口已从占位改为真实上传入口，并继续复用现有附件消息链路。
- 图片 / 文件 / 音视频统一接入预览与媒体展示能力。
- 会话未读标题、桌面通知、免打扰过滤与 `@` 提醒优先级策略已闭环。

### 阶段结论

- `Stage 9` 已完成，富媒体、互动与通知体验达到当前仓库范围内的成熟 IM 目标。
- 下一阶段锚点为 `Stage 10 / 多设备、账号与平台能力`，现也已完成。

## Stage 10 完成记录

### 当前目标

- 用户资料编辑能力
- 头像、昵称、签名编辑闭环
- 在线状态与会话摘要
- 设备标识与设备摘要展示
- 异常登录能力预留
- 全局消息搜索与跨会话跳转
- 附件中心 / 全局媒体聚合

### 已完成内容

- 资料编辑能力已接入前后端闭环，支持头像、昵称、签名修改。
- 已新增会话摘要接口，可查看当前设备标识、在线状态、活跃设备数量等信息。
- 已新增全局消息搜索入口，支持跨会话查看并跳转到目标消息。
- 已新增附件中心能力，支持全局图片 / 文件聚合查看并回跳原始消息。
- 已在配置初始化阶段提前加载 `.env`，修复 Redis 在直接模块导入或测试路径下回落到默认 `127.0.0.1:6379` 的问题。

### 阶段结论

- `Stage 10` 已完成，当前仓库范围内的成熟 IM 产品 0-10 阶段计划已全部闭环。
- 后续新增需求应作为计划外迭代管理，而不是重新打开当前 0-10 阶段主线。

## 实施顺序

1. `Stage 0` 到 `Stage 10` 已全部完成。
2. 后续如继续演进，应以新增需求、回归缺陷或平台扩展为入口，不再按未完成阶段推进。
3. 若出现编码、环境或文档问题，应优先修复基础设施问题，再继续功能迭代。

## 计划外迭代草案：Electron 实时音视频通话

### 迭代定位

- 本迭代为 `Stage 0-10` 完成后的计划外产品演进。
- 目标不是继续扩展“附件消息入口”，而是补齐真正的实时音视频通话能力。
- 首发平台为 `Electron`。
- `mobile` 暂不实现。
- 技术实现细节单独维护在 `docs/realtime-calling-technical-design.md`，本计划仅保留目标、步骤、状态与结论。
- 详细开发任务拆解单独维护在 `docs/realtime-calling-implementation-plan.md`。

### 已确认需求

- 支持单聊实时语音通话。
- 支持单聊实时视频通话。
- 支持群聊实时语音 / 视频通话。
- 支持直接发送语音消息。
- 单条语音消息最长 `60` 秒。
- 实时通话不设产品时长上限。
- 输入栏交互约定：
  - 麦克风按钮用于发起语音通话
  - 摄像头按钮用于发起视频通话
  - 语音消息采用“按住说话”
  - 松开发送
  - 不做上滑取消
  - 录音中显示波形和时长
  - 麦克风权限被拒绝时提示开通权限
- 来电 `45` 秒未接听自动超时。
- 同账号全部在线设备同时振铃。
- 用户处于通话中时，新的来电直接忙线。
- 每次通话结束后生成一条通话记录消息。
- 首版目标包含弱网可用与 `TURN` 中继支持。

### 当前实际进展

- `Iteration A` 已完成：
  - 通话域、状态机、信令方向与 `TURN` / `SFU` 技术路线已冻结。
- `Iteration B` 已完成：
  - 单聊实时语音通话已具备可用闭环，服务端状态机、`WebSocket` 信令、`WebRTC P2P`、多设备振铃与通话记录消息已打通。
- `Iteration C` 已完成：
  - 单聊实时视频通话已具备可用闭环，前端已支持本地预览、远端视频渲染与设备能力降级，当前单聊能力已完成实际验证。
- `Iteration D` 已完成：
  - 群聊实时语音 / 视频通话首版闭环已经形成，当前项目状态已按已完成维护。
- `Iteration E` 已完成：
  - 语音消息能力当前已按已完成维护，作为本轮独立能力纳入完成范围。
- `Iteration F` 已部分落地：
  - `GET /auth/call/ice`、动态 ICE/TURN 配置、共享密钥 TURN 凭证、调试开关与客户端消费链路已具备，但 `coturn` 实网部署与 relay 验证仍待完成。

### 本迭代明确不做

- `mobile` 端通话实现
- 屏幕共享
- 通话录制
- 美颜、背景虚化、降噪增强等高级媒体处理
- 直播、举手、主持人控场、旁听模式
- 跨端无缝切换中的完整迁移体验

### 正式实施阶段草案

#### Iteration A：需求冻结与架构确认

- 目标：
  - 冻结本轮产品范围与验收边界
  - 明确单聊、群聊、多设备、超时、忙线与记录规则
- 输出：
  - 正式需求说明
  - 正式技术设计文档
  - 正式实施排期
- 当前状态：
  - `complete`
- 当前结论：
  - 核心技术路线已确认：
    - 单聊 `P2P`
    - 群聊 `SFU`
    - 自建 `coturn`
    - 服务端权威状态
    - 单用户单设备接通
- 阶段完成标志：
  - 所有核心状态与边界条件有书面定义

#### Iteration B：单聊实时语音通话闭环

- 目标：
  - 完成 Electron 单聊语音通话第一版闭环
- 关键步骤：
  - 发起与接听
  - 拒绝、取消、忙线、超时
  - 通话中状态展示
  - 结束后记录沉淀
- 当前状态：
  - `complete`
- 阶段完成标志：
  - 单聊语音通话在 Electron 上稳定闭环

#### Iteration C：单聊实时视频通话闭环

- 目标：
  - 完成 Electron 单聊视频通话第一版闭环
- 关键步骤：
  - 视频呼叫入口与来电处理
  - 通话中画面展示
  - 语音 / 视频状态切换
  - 结束后记录沉淀
- 当前状态：
  - `complete`
- 阶段完成标志：
  - 单聊视频通话闭环稳定

#### Iteration D：群聊实时通话首版闭环

- 目标：
  - 完成群聊实时语音 / 视频通话首版闭环
- 关键步骤：
  - 群内发起通话
  - 成员振铃与加入
  - 成员拒绝、忙线、超时
  - 成员离开与通话结束
  - 群通话结果沉淀
- 当前状态：
  - `complete`
- 当前结论：
  - 当前仓库已经接入 `LiveKit` 群聊媒体层。
  - 群聊语音 / 视频通话首版闭环已经形成，群通话状态机、成员离开规则与通话记录链路已对齐当前实现。
  - 当前自动化真实验证覆盖到服务端业务链路、`LiveKit` token / 地址下发与群通话结束规则；真实浏览器 / Electron 客户端入房联调留在发布前验收阶段执行。
- 阶段完成标志：
  - 群聊通话可在可控规模内稳定运行

#### Iteration E：语音消息闭环

- 目标：
  - 完成类似成熟 IM 的直接语音消息能力
- 关键步骤：
  - 麦克风录制入口
  - `60` 秒时长限制
  - 录制完成后发送、播放与失败重试
  - 会话中的语音消息展示
- 当前状态：
  - `complete`
- 当前结论：
  - 语音消息当前已按已完成维护。
  - 后续重点不再是语音消息主闭环，而是围绕 `coturn`、弱网与发布收口继续推进。
- 阶段完成标志：
  - 语音消息在 Electron 上形成独立闭环

#### Iteration F：弱网、TURN 与可靠性补强

- 目标：
  - 提升复杂网络环境下的接通率与稳定性
- 关键步骤：
  - 补齐弱网场景验证
  - 接入 `TURN` 相关配置与部署准备
  - 完善异常场景处理与用户提示
- 当前状态：
  - `in_progress`
- 当前结论：
  - 动态 ICE/TURN 配置签发、客户端获取与 relay 调试开关已具备。
  - 剩余重点是 `coturn` 部署、真实 relay 验证与弱网场景回归。
- 阶段完成标志：
  - 弱网与 NAT 场景下仍具备较高接通率

#### Iteration G：验证、运维与发布准备

- 目标：
  - 形成可验证、可发布、可排障的首发版本
- 关键步骤：
  - 补齐测试与回归清单
  - 沉淀运维文档
  - 补齐关键指标与发布检查项
- 当前状态：
  - `pending`
- 阶段完成标志：
  - 具备首发上线与排障基础

### 验收标准草案

- 单聊语音：
  - 发起、接听、拒绝、取消、忙线、超时全部可用
  - 通话双方均可听到对方声音
- 单聊视频：
  - 发起、接听、拒绝、取消、忙线、超时全部可用
  - 双方可看到对方画面并可切换摄像头开关状态
- 群聊通话：
  - 可从群会话发起
  - 多成员可加入 / 离开
  - 成员状态可见
- 语音消息：
  - 支持直接录制并发送
  - 单条最长 `60` 秒
  - 发送后可在会话中播放
- 多设备：
  - 同账号多设备可同时振铃
  - 某一设备接通后，其余设备进入已处理状态
- 记录：
  - 每次通话结束后都能在会话中留下可读记录
- 网络：
  - 常见 NAT 环境下可通过 `TURN` 建立通话
  - 弱网下优先保证音频可用

### 当前建议实施顺序

1. 优先推进 `Iteration F`，完成 `coturn` 部署、`TURN` relay 实网验证与弱网验证。
2. 收口 `Iteration G`，沉淀群聊通话、语音消息与弱网能力的回归、运维与发布材料。

### 当前结论

- 当前已完成 `Iteration A`、`Iteration B`、`Iteration C`、`Iteration D`、`Iteration E`。
- 当前功能上的下一步是 `Iteration F / TURN 与可靠性补强`，随后进入 `Iteration G / 验证、运维与发布准备`。
- 本计划已经同步为当前代码状态，不再把单聊音视频通话记为“未开始”。
- 详细技术实现、协议、数据模型与风险规避方案统一维护在 `docs/realtime-calling-technical-design.md`。

## 计划外迭代草案：React Native 移动端（Android / iOS）

### 迭代定位

- 本迭代为 `Stage 0-10` 完成后的新增产品线，不回退到旧阶段管理。
- 目标是建设 `Android` 与 `iOS` 同步开发的 `React Native` 移动端，功能上与桌面端几乎一致。
- 当前 `apps/mobile` 已切换为真正的 `React Native` 原生双端工程，旧 `Capacitor` 壳、空包和遗留配置已移除。
- 专项技术方案、架构和里程碑细节统一维护在 `docs/mobile-react-native-technical-plan.md`。

### 已确认原则

- Android 与 iOS 同步推进，不采用“先单平台做完再移植”的节奏。
- 业务协议、领域模型、同步策略、通话状态机尽可能复用现有共享层。
- Web / Electron 组件不直接复用到移动端界面。
- UI 采用流行移动 IM 风格，参考 `WhatsApp`、`Telegram` 一类产品的导航、密度和手势习惯，但不做桌面界面的缩放移植。
- 首版仍然以代码质量、可维护性、可扩展性为前提，不以“快速堆页面”替代架构收口。

### 当前实际进展

- `Iteration M0` 已完成：
  - 仓库现状与移动端可复用资产已核查。
  - `Capacitor -> React Native` 的迁移方向已冻结。
  - 移动端专项方案文档已建立。
- `Iteration M1` 已完成：
  - 已新增 `@mushroom/app-core`，承载移动端共享应用层、鉴权、同步和本地仓储接口。
  - 已把 `apps/mobile` 重建为 `React Native` 原生双端工程，并接通 workspace、Metro、Jest 与原生宿主。
- `Iteration M2` 已完成：
  - 已接入登录 / 注册、设备注册、token 刷新、启动同步、消息状态同步与本地持久化。
  - 已在 React Native 原生宿主中跑通移动端启动、冷启动恢复和同步预览主链路。
- `Iteration M3` 已完成：
  - 已接通会话列表、联系人入口、聊天详情、文本发送、草稿保存、实时收消息与已读同步。
  - 已形成移动端 `Chats / Contacts / Me` 三段式主路径，并支持从联系人进入单聊。
- `Iteration M4` 已完成：
  - 已接通图片 / 文件附件选择、上传、发送与图片预览。
  - 已接通回复、转发、撤回、收藏、置顶、会话内搜索与本地清空消息能力。
  - 已把 `message_recall` 与消息状态更新接入移动端共享 controller，并完成 React Native 页面动作面板闭环。
- 当前下一步：
  - 进入 `Iteration M5`，补齐资料、治理、多设备与全局能力。

### 当前范围

- 登录 / 注册
- 联系人与群
- 会话列表
- 单聊 / 群聊
- 文本 / 图片 / 文件 / 语音消息
- 撤回、回复、转发、收藏、置顶、搜索
- 用户资料、设备摘要、联系人 / 群治理
- 实时音视频通话
- 推送、后台同步与移动端发布能力

### 正式实施阶段草案

#### Iteration M0：方案冻结与仓库迁移准备

- 目标：
  - 冻结移动端技术路线与复用边界
  - 明确主计划跟踪方式
- 输出：
  - 专项技术方案文档
  - 主计划关键里程碑
- 当前状态：
  - `complete`
- 阶段完成标志：
  - 已明确 `React Native` 原生双端路线

#### Iteration M1：移动端基座与共享层抽取

- 目标：
  - 重建 `apps/mobile`
  - 抽取平台无关应用域逻辑
- 关键步骤：
  - 建立 React Native 工程基座
  - 接通 workspace 引用
  - 新增共享应用域层
  - 补齐 lint / type-check / test 基线
- 当前状态：
  - `complete`
- 当前结论：
  - `apps/mobile` 已完成 React Native 原生工程替换，Android / iOS 宿主、Metro、Babel、Jest、workspace 引用与共享应用层边界已经接通。
  - 旧 `Capacitor`、`Ionic`、`packages/ui`、`packages/native-bridge` 遗留已清理，不再保留伪复用层。
- 阶段完成标志：
  - Android / iOS 真机或模拟器可稳定启动

#### Iteration M2：登录、启动与同步基座

- 目标：
  - 打通账号体系、本地持久化与同步主链路
- 关键步骤：
  - 登录 / 注册
  - token 与设备注册
  - 会话 / 消息本地存储
  - 启动、重连、前后台同步
- 当前状态：
  - `complete`
- 当前结论：
  - 登录 / 注册、设备注册、token 刷新、启动同步、好友 / 会话 / 消息 / 消息状态同步与本地持久化已在 React Native 原生宿主中接通。
  - 冷启动恢复、登录后同步、手动同步和退出清理已形成闭环，后续进入 `M3` 扩展聊天主路径。
- 阶段完成标志：
  - 冷启动与重连恢复闭环

#### Iteration M3：聊天主路径闭环

- 目标：
  - 完成移动端 IM 日常主路径
- 关键步骤：
  - 会话列表
  - 联系人 / 群列表
  - 单聊 / 群聊文本消息
  - 已读、草稿、置顶、免打扰
- 当前状态：
  - `complete`
- 当前结论：
  - 会话列表、联系人列表、聊天详情、文本消息发送、实时消息接收、草稿保存和会话已读已在 React Native 原生宿主中接通。
  - 当前移动端已经具备稳定的基础聊天主路径，可继续进入媒体消息和成熟 IM 动作补齐。
- 阶段完成标志：
  - 主聊天路径可稳定日常使用

#### Iteration M4：媒体消息与成熟 IM 动作

- 目标：
  - 补齐高频消息能力
- 关键步骤：
  - 图片 / 视频 / 文件
  - 回复、撤回、转发、收藏、置顶
  - 搜索与消息状态同步
- 当前状态：
  - `complete`
- 当前结论：
  - 图片 / 文件附件链路已在 React Native 原生宿主接通，并支持上传后发送和图片预览。
  - 消息动作已接通回复、转发、撤回、收藏、置顶、本地清空与会话内搜索。
  - `@mushroom/app-core` 已补齐本地搜索、消息状态更新、撤回处理和 `message_recall` 实时事件应用。
- 阶段完成标志：
  - 高优先级消息能力与桌面端大体对齐

#### Iteration M5：资料、治理与多设备能力

- 目标：
  - 补齐移动端可长期使用能力
- 关键步骤：
  - 用户资料编辑
  - 设备摘要
  - 删除好友、拉黑、群治理
  - 全局搜索、附件中心
- 当前状态：
  - `complete`
- 当前结论：
  - React Native 移动端已经接通资料编辑、设备摘要、安全动态、全局搜索与附件中心。
  - 联系人治理已支持删除好友、拉黑、解除拉黑。
  - 群治理已支持群资料、群公告、群设置、邀请成员、角色调整、禁言、移出成员、转让群主与退群。
  - `@mushroom/app-core` 已补齐资料、多设备、联系人治理、群治理与附件聚合查询的共享控制层。
- 阶段完成标志：
  - 功能面接近桌面端成熟能力

#### Iteration M6：语音消息与实时音视频通话

- 目标：
  - 完成移动端语音消息与通话闭环
- 关键步骤：
  - 按住说话
  - 单聊实时语音 / 视频
  - 群聊实时语音 / 视频
  - 通话中状态与记录沉淀
- 当前状态：
  - `complete`
- 当前结论：
  - React Native 移动端已经接通按住说话、语音录制、语音上传发送、语音播放、时长展示与波形展示。
  - 移动端已经接入 `call.*` 实时通话信令，覆盖发起、响铃、接听、拒绝、挂断、忙线、超时、媒体状态切换与状态同步。
  - 单聊通话已接通直连场景所需的 ICE / TURN 配置拉取，群聊通话已接通 `LiveKit` 房间配置拉取与会话内展示。
  - 通话浮层已经沉淀会话状态、参与者状态、本地音频/视频开关、通话阶段文案与前台在线场景下的会话收敛。
  - 当前 `M6` 完成边界为前台在线场景下的语音消息与通话主链路；后台来电、推送唤醒、系统级通知与真机构建收口继续放在 `M7`。
- 阶段完成标志：
  - 前台在线场景下的语音消息与通话稳定闭环

#### Iteration M7：通知、后台能力与发布准备

- 目标：
  - 形成真实可交付的移动端首版
- 关键步骤：
  - `M7-A` 推送链路接通
  - `M7-B` 系统级来电提醒与接听入口
  - `M7-C` 前后台 / 弱网恢复与状态补齐
  - `M7-D` 回归清单、发布材料与原生依赖收口
- 当前状态：
  - `in_progress`
- 当前结论：
  - React Native 移动端已经接入 `Firebase Messaging`、`Notifee`、`CallKeep` 与 `NetInfo`，补齐消息推送、系统来电展示、通知点击恢复、通话状态回补与断网重连恢复骨架。
  - 移动端现已补齐统一推送抽象层，客户端与服务端都支持 `FCM + Huawei + Xiaomi` 三 Provider 路由与设备注册字段扩展，不再是单一 `FCM` 直连实现。
  - 服务端已经补齐 `push.notification` 出站事件、聊天消息推送载荷与来电推送载荷，消息与通话从“仅前台 WebSocket”扩展为“前台实时 + 后台推送”双链路。
  - Android 侧已经补齐 `Xiaomi` 原生桥接、`regId` 注册同步、Manifest 占位符与本地 `AAR` 接入位，服务端补齐 `XiaomiPushProvider` 与 Java helper 对接点。
  - Android / iOS 原生权限声明、测试 mock、回归清单文档已经补齐，代码侧 `M7-A/B/C/D` 已完成。
  - Electron 桌面端已经补齐主进程通知协调层、任务栏闪烁、原生系统通知、来电专用提醒、通知点击回流与会话/通话收口逻辑。
  - 当前剩余工作主要是真实 `FCM/APNs/Huawei/Xiaomi` 凭据与 SDK、真机来电/通知、签名环境与发布前演练，不再是业务能力缺口。
- 阶段完成标志：
  - 真实设备与发布环境验证完成后，可将状态切换为 `complete`

### 当前建议实施顺序

1. 继续执行 `Iteration M7` 的真机与发布环境验证，把当前代码收口推进到可发布演练状态。
2. 在 Android 真机上验证 `FCM`、`Huawei` 与 `Xiaomi` 三条推送链路的 token / regId 注册、消息通知、来电提醒与通知点击恢复。
3. 在 iOS 真机上完成 `FCM/APNs` 凭据、CallKeep 后台来电与签名发布配置演练。
4. 完成真机通知 / 来电 / 弱网恢复回归后，将 `M7` 切换为 `complete`。

### 当前结论

- 当前移动端工作已经进入正式方案阶段，不再维持“仅保留壳层能力”的旧定位。
- 当前主线上的 `Iteration M6 / 语音消息与实时音视频通话` 已完成。
- 当前主线上的 `Iteration M7 / 通知、后台能力与发布准备` 已完成代码实现，当前下一步是真机通知、系统来电与发布环境演练。
- 当前 Electron 桌面端已经具备基于现有 IM 长连接的普通消息桌面提醒与音视频来电提醒骨架。
- 当前移动端多 Provider 推送抽象、服务端 `PushProvider` 路由器与 `FCM + Huawei + Xiaomi` 三通道骨架已经补齐，下一步主要是真机和云侧环境验证。
- 专项架构、技术栈、复用边界与风险规避统一维护在 `docs/mobile-react-native-technical-plan.md`。
